/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0 and the Server Side Public License, v 1; you may not use this file except
 * in compliance with, at your election, the Elastic License 2.0 or the Server
 * Side Public License, v 1.
 */
import * as rt from 'io-ts';
export var isEmptyString = function isEmptyString(value) {
  return value === '';
};
export var containsSpaces = function containsSpaces(value) {
  return value.includes(' ');
};
export var containsEmptyEntries = function containsEmptyEntries(value) {
  return value.split(',').some(isEmptyString);
};
export var validateIndexPattern = function validateIndexPattern(indexPattern) {
  return !isEmptyString(indexPattern) && !containsSpaces(indexPattern) && !containsEmptyEntries(indexPattern);
};
export var indexPatternRt = rt.brand(rt.string, function (pattern) {
  return validateIndexPattern(pattern);
}, 'IndexPattern');